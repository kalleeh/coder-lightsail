# =============================================================================
# Coder Lightsail - Environment Configuration
# =============================================================================
# Copy this file to .env and fill in the values before running docker-compose.
#
#   cp .env.example .env
#
# IMPORTANT: Never commit .env to version control.
# =============================================================================

# -----------------------------------------------------------------------------
# Domain & Access
# -----------------------------------------------------------------------------

# The domain name pointing to this Coder instance (no protocol prefix).
DOMAIN=coder.example.com

# Full URL used by Coder to generate links, webhooks, etc.
# Must match the domain above.
CODER_ACCESS_URL=https://coder.example.com

# Docker group GID on the host. setup.sh detects this automatically.
# Find manually with: getent group docker | cut -d: -f3
DOCKER_GID=999

# Coder server version. Pin to a specific version for stability.
# Check releases: https://github.com/coder/coder/releases
CODER_VERSION=latest

# -----------------------------------------------------------------------------
# PostgreSQL Database
# -----------------------------------------------------------------------------

# Database user for Coder's PostgreSQL instance.
POSTGRES_USER=coder

# Database password. Leave as-is and setup.sh will auto-generate a secure one.
# Or set your own strong random value, e.g.:  openssl rand -base64 32
POSTGRES_PASSWORD=CHANGE_ME_USE_A_STRONG_RANDOM_PASSWORD

# Database name.
POSTGRES_DB=coder

# -----------------------------------------------------------------------------
# AWS / Bedrock Configuration
# -----------------------------------------------------------------------------

# AWS region where Bedrock models are available.
AWS_REGION=us-east-1

# AWS credentials for Bedrock access.
# Optional if the host uses an IAM instance role / instance profile.
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=

# Default Bedrock model used by the AI Bridge.
CODER_AIBRIDGE_BEDROCK_MODEL=anthropic.claude-sonnet-4-5-20250929-v1:0

# Comma-separated list of Bedrock models users can choose from.
CODER_AIBRIDGE_MODELS=anthropic.claude-sonnet-4-5-20250929-v1:0,anthropic.claude-opus-4-5-20251101-v1:0,anthropic.claude-sonnet-4-20250514-v1:0
